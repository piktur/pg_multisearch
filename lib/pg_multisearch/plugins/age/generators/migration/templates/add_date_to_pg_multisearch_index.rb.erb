class AddDateToPgMultisearchIndex < ActiveRecord::Migration
  def up
    add_column :pg_search_documents, :<%= options[:column] %>, :datetime
    add_index  :pg_search_documents, :<%= options[:column] %>
  end

  def down
    remove_index  :pg_search_documents, :<%= options[:column] %>
    remove_column :pg_search_documents, :<%= options[:column] %>
  end
end
