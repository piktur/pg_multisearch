class AddDateToPgMultisearchIndex < ActiveRecord::Migration
  def up
    add_column :pg_multisearch_documents, :<%= options[:column] %>, :datetime
    add_index  :pg_multisearch_documents, :<%= options[:column] %>
  end

  def down
    remove_index  :pg_multisearch_documents, :<%= options[:column] %>
    remove_column :pg_multisearch_documents, :<%= options[:column] %>
  end
end
