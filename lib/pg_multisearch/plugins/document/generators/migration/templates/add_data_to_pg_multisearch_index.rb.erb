class AddDataToPgMultisearchIndex < ActiveRecord::Migration
  def up
    add_column :pg_multisearch_documents, :data, :jsonb
<% if options[:index] -%>
    add_index :pg_multisearch_documents, :data, using: :gin
<% end -%>
  end

  def down
    remove_column :pg_multisearch_documents, :data
    remove_index :pg_multisearch_document, :data if index_exists?(:pg_multisearch_document, :data)
  end
end
